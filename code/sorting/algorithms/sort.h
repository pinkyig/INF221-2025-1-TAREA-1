#ifndef SORT_H
#define SORT_H

#include <vector>

// Declaración de la función para ordenar usando la función de librería, o una envoltura.
std::vector<int> sortArray(std::vector<int>& arr);

#endif // SORT_H
